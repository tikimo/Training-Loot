package TestiTrain;

public class intro {
	
	// An array of positive integers is given. Implement a function that
	// returns the smallest sum of any two numbers in the array.
	// Let’s use Java 8 Streams instead of loops. You can use any tools
	// and technologies (including Google :)) that you want.
	 public static void main(String[] args) {
		 int te = 534;
		 print(te);
		 int leen = (int) Math.pow(te, Integer.toString(te).length());
		 te = te/leen*leen;
		 print(te);
		 
	}
	
	 public static void print(int a) {
		 System.out.println(a);
	 }

}
